// Supabase runtime config example.
// Copy this shape only after the runtime config strategy is finalized.
// Do not commit a real Project URL, anon key, publishable key, service_role,
// secret value, DB password, or token.

window.VELGARD_SUPABASE_CONFIG = {
  url: "",
  anonKey: ""
};
